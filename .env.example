# VARIABLES DE ENTORNO - Marketplace Link Frontend
# IMPORTANTE: Estas variables se configuran en RUNTIME, no en build time
# El sistema usa docker-entrypoint.sh para inyectar las variables al iniciar el contenedor

# URL del backend API (se inyecta en runtime via config.js)
VITE_API_URL=http://localhost:8080

# URL del frontend (solo para referencia, no critica)
VITE_FRONTEND_URL=http://localhost:5174

# Puerto en el que se expondra el frontend en el host (Docker)
FRONT_PORT=5174